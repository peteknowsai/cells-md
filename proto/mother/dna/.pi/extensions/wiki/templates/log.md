# Wiki log

Chronological record of writes, lints, and dream ingestions. Entries
are appended at the end of the file. Each entry is a `## [timestamp]`
heading followed by structured content. The dream tool uses the most
recent ingest entry's timestamp as its cursor (next ingest only
processes session events newer than that).

