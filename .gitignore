state/
.env
.env.local
.envrc
*.log
.DS_Store
node_modules/
dist/
build/
.pi/.cache/
.pi/npm/
# only the top-level memory dir (cell-keeper's data) — NOT the memory extension code under template/.pi/
/memory/

# .claude/ is mostly committed (Pete Loop infra: hooks, commands, loops).
# Runtime state + per-machine settings are not.
.claude/.pete-loop.active
.claude/scheduled_tasks.lock
.claude/settings.json
NEEDS_PETE.md
