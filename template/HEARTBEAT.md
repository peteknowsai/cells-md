# Heartbeat

## Daily

- 04:00 local — `dream` to consolidate memory, wiki, and mentality.

## Notes

You can't wake yourself up — Sprites hibernate when idle. The schedule
above is a contract. Today, the launchd plist that `cells schedule-dreams`
installs on Pete's Mac fires `cells dream __NAME__` at the declared time.

A future heartbeat agent (a sibling on Pete's Mac) will read this file
across all cells and replace the per-launchd plumbing with one
declarative system. To add a wake-up, write a line here describing
when and what should happen.
