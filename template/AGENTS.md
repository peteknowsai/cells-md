# __NAME__

Stub. The real agent identity lives at `.pi/agents/self.md` — that's what
gets loaded as the system prompt by the `identity` extension at runtime. If
you (the agent) ever read this file instead, something is misconfigured —
check that `node_modules/` exists and `.pi/extensions/identity/index.ts` is
present.
